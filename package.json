{
  "name": "tailwind-assingment",
  "version": "1.0.0",
  "description": "Task: Substituting the existing CSS in the provided project with a Tailwind implementation.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"npx tailwindcss -i ./input.css -o ./output.css",
    "watch":"npx tailwindcss -i ./input.css -o ./output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
